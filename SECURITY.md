# Seguridad

Este repositorio público no debe contener:

- credenciales reales de MySQL;
- claves API reales;
- archivos `.env`;
- `web/conexion.php` con datos de producción;
- exportaciones de bases de datos.

La clave usada por la estación debe configurarse fuera del código y rotarse si alguna vez fue publicada.
